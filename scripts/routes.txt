1. Everything is done with:
    String operations (regex, splitting, cleaning)
    Simple scoring rules (counting keywords, sentence length)
        tatic question templates

2. using encoder and decoder - T5, Bart or Flan t5

3. combination on 1,2 and llama 3 for clean up. git remote -v

4. combination of 2 and Llama 3 for clean up

5. Just Llama - Not possible.  - Synthetic

6. Webscraping with or without Llama

7. Information Extraction (IE) - Use NER + dependency parsing to extract key relations, then convert to Q&A

8. Embedding-based Clustering + Summarization - Cluster semantically similar sentences, summarize each cluster into Q&A

9. RAG ??? with pymupdf.  

