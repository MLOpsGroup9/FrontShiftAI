name: Chat Pipeline - full eval Mac Self-Hosted Check

on:
  workflow_dispatch:

jobs:
  mac-self-hosted:
    runs-on: [self-hosted, macos]
    timeout-minutes: 120

    env:
      LLAMA_MODEL_PATH: /Users/sriks/Documents/Projects/FrontShiftAI/models/Llama-3.2-3B-Instruct-Q4_K_M.gguf
      GENERATION_BACKEND: local
      TOKENIZERS_PARALLELISM: false
      CHAT_PIPELINE_ALLOW_HEAVY_FALLBACKS: 1
      CHAT_PIPELINE_EXECUTION_MODE: local

    steps:
      # -------------------------------
      # Checkout Code
      # -------------------------------
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          lfs: true

      # -------------------------------
      # Python Setup
      # -------------------------------
      - name: Validate Python
        run: |
          echo "Using system Python:"
          which python3
          python3 --version

      - name: Install Dependencies
        run: |
          python3 -m pip install --upgrade pip
          pip3 install -r chat_pipeline/requirements.txt || true
          pip3 install llama-cpp-python
          pip3 install pyyaml

      - name: Export PYTHONPATH
        run: |
          echo "PYTHONPATH=$(pwd)" >> $GITHUB_ENV

      # -------------------------------
      # GCP Authentication
      # -------------------------------
      - name: Auth to GCP
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up gcloud SDK
        uses: google-github-actions/setup-gcloud@v2

      # -------------------------------
      # Sync Vector DB (NO MULTIPROCESSING / NO MULTITHREADING)
      # -------------------------------
      - name: Sync vector DB from GCS (single-threaded)
        run: |
          mkdir -p chat_pipeline/data_pipeline/data/vector_db

          gsutil \
            -o "GSUtil:parallel_process_count=1" \
            -o "GSUtil:parallel_thread_count=1" \
            rsync -r \
            gs://frontshiftai-data/data/vector_db \
            chat_pipeline/data_pipeline/data/vector_db

          echo "CHROMA_DIR=chat_pipeline/data_pipeline/data/vector_db" >> $GITHUB_ENV

      # -------------------------------
      # W&B Setup
      # -------------------------------
      - name: Set W&B Environment Variables
        run: |
          echo "WANDB_ENTITY=group9mlops-northeastern-university" >> $GITHUB_ENV
          echo "WANDB_PROJECT=FrontShiftAI" >> $GITHUB_ENV

      # =====================================================
      #                  CORE EVAL
      # =====================================================

      - name: Generate Core Evaluation Responses
        env:
          WANDB_API_KEY: ${{ secrets.WANDB_API_KEY }}
        run: |
          python3 -m chat_pipeline.cli \
            --config chat_pipeline/configs/experiments/core_eval_self_hosted.yaml \
            --mode generate \
            --debug

      - name: Judge Core Evaluation Responses
        env:
          WANDB_API_KEY: ${{ secrets.WANDB_API_KEY }}
        run: |
          python3 -m chat_pipeline.cli \
            --config chat_pipeline/configs/experiments/core_eval_self_hosted.yaml \
            --mode judge \
            --debug || true

      - name: Verify Core Eval (Relaxed)
        run: |
          echo "🔍 Listing evaluation results:"
          ls -R chat_pipeline/results || true

          MAIN_EXAMPLES="chat_pipeline/results/core_eval_main/examples.jsonl"
          SLICES_EXAMPLES="chat_pipeline/results/core_eval_slices/examples.jsonl"
          EXP_SUMMARY="chat_pipeline/results/core_experiment_summary_self_hosted.json"

          if [ -f "$MAIN_EXAMPLES" ] || \
             [ -f "$SLICES_EXAMPLES" ] || \
             [ -f "$EXP_SUMMARY" ]; then
              echo "✅ Core eval produced output files."
              exit 0
          fi

          echo "❌ No core eval output detected — failing."
          exit 1

      # =====================================================
      #                  FULL EVAL
      # =====================================================

      - name: Generate Full Evaluation Responses
        env:
          WANDB_API_KEY: ${{ secrets.WANDB_API_KEY }}
        run: |
          python3 -m chat_pipeline.cli \
            --config chat_pipeline/configs/experiments/full_eval_self_hosted.yaml \
            --mode generate \
            --debug

      - name: Judge Full Evaluation Responses
        env:
          WANDB_API_KEY: ${{ secrets.WANDB_API_KEY }}
        run: |
          python3 -m chat_pipeline.cli \
            --config chat_pipeline/configs/experiments/full_eval_self_hosted.yaml \
            --mode judge \
            --debug || true

      - name: Verify Full Eval (Relaxed)
        run: |
          echo "🔍 Listing evaluation results:"
          ls -R chat_pipeline/results || true

          F_MAIN="chat_pipeline/results/full_eval_main/examples.jsonl"
          F_SLICES="chat_pipeline/results/full_eval_slices/examples.jsonl"

          if [ -f "$F_MAIN" ] || \
             [ -f "$F_SLICES" ]; then
              echo "✅ Full eval produced output files."
              exit 0
          fi

          echo "❌ No full eval output detected — failing."
          exit 1

      # -------------------------------
      # Upload Artifacts
      # -------------------------------
      - name: Upload Evaluation Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: all-eval-results
          path: |
            chat_pipeline/results/
            models/
          retention-days: 30
